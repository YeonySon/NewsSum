spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://j9b202.p.ssafy.io:3350/newsum?useUniCode=yes&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: ssafy
    #username: newsum
    #password: k3s2b202ssafy

  jpa:
    show-sql: true
    database: mysql
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

# JPA log
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE

server:
  port: 8811
  max-http-header-size: 2MB

